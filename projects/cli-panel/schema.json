{
  "id": "CliPanelBuilderSchema",
  "title": "Cli Panel builder",
  "description": "Cli Panel builder options",
  "properties": {
    "properties": {
      "format": {
        "type": "string",
        "description": "Timestamp format",
        "default": "dd/mm/yyyy"
      },
      "path": {
        "type": "string",
        "description": "Path to the timestamp file",
        "default": "./timestamp"
      }
    }
  }
}
