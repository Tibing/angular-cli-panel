{
  "properties": {
    "raw": {
      "type": "boolean",
      "description": "Disables CLI Panel and shows raw log.",
      "default": false
    },
    "ebport": {
      "type": "number",
      "description": "Port to setup websocket."
    },
    "mode": {
      "type": "string",
      "description": "Builder execution mode.",
      "default": "panel",
      "enum": [
        "panel",
        "builder"
      ]
    }
  }
}
