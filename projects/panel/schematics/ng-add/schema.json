{
  "$schema": "http://json-schema.org/schema",
  "id": "ng-add",
  "title": "Angular CLI Panel Builder ng-add schematics",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
